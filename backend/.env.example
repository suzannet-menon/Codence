# === Server ===
PORT=5000
FRONTEND_URL=http://localhost:3000

# === GitHub ===
# Personal Access Token with repo scope — used to fetch PR diffs via GitHub API
GITHUB_TOKEN=

# Secret configured in the GitHub webhook settings (leave blank to skip HMAC validation)
GITHUB_WEBHOOK_SECRET=

# === AI / Gemini ===
GEMINI_API_KEY=your_key_here

# === ChromaDB (Person B) ===
CHROMA_URL=http://localhost:8000
